const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'classroom'
    },
    socketPort:'3000',
    httpPort:'2000'
};

module.exports.config = config;